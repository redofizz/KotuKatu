<p>ようこそ、 <%= @email %>!</p>

<p>次のリンクでメールアドレスの確認が完了します:</p>

<p><%= link_to 'アカウント確認', confirmation_url(@resource, confirmation_token: @token, config: message['client-config'].to_s, redirect_url: message['redirect-url'].to_s) %></p>
